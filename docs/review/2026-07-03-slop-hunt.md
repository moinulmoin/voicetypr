# Slop-hunt findings — 2026-07-03 (ultracode review of integrate/perf-ux-2026-07)

Every CONFIRMED finding survived 2 adversarial verifiers. DISPUTED = 1 of 2 refuted; evaluate before fixing.

## CONFIRMED

### [critical] src/pill.css:87 — Author display rules defeat the hidden attribute: all pill states render simultaneously and the surface never hides in-page

pill.tsx hides state rows and the surface purely via `element.hidden` (src/pill.tsx:207-209, 270-277), which relies on the UA stylesheet rule `[hidden]{display:none}`. But pill.css assigns author-origin display values to every hidden-toggled element: `.pill-surface{display:flex}` (line 35) and `.pill-dots,.pill-bars,.pill-status{display:flex}` (lines 84-90). Author-origin declarations override UA-origin `[hidden]` regardless of specificity, so in the real webview `hidden` has no effect on these elements. Only `.pill-preview` got an explicit `.pill-preview[hidden]{display:none}` guard (lines 161-163). Runtime effect: during any recording the pill shows the idle dots, listening controls, 'Transcribing…' spinner, 'Polishing…' spinner and the (empty) error row all stacked at once; and with pill_indicator_mode='always' the dark surface can never be hidden by the page. The test suite passes because RecordingPill.test.tsx runs in jsdom with `toBeVisible()`, which only inspects the hidden attribute/inline styles — pill.css is never applied, so the regression is invisible to CI.

> VERIFIER: Confirmed, not refuted. pill.tsx hides the surface and all state rows solely via element.hidden (setHidden, lines 207-209; render, lines 270-277), which depends on the UA-origin rule [hidden]{display:none}. pill.css assigns author-origin display values to every one of those elements: .pill-surface{display:flex} (line 35) and .pill-dots,.pill-bars,.pill-status{display:flex} (lines 84-90). Author-origin normal declarations override UA-origin declarations regardless of specificity, so hidden has no effect on them in a real webview; only .pill-preview has an explicit .pill-preview[hidden]{display:none} guard (lines 161-163). pill.html loads only pill.tsx/pill.css — no Tailwind preflight or other [hidden] rule exists in the pill window (unlike main, where pill.tsx imported globals.css with Tailwind's [hidden]{display:none!important}), so the regression is introduced by this branch's micro-bundle rewrite. Plans 031/039 do not document this as an intentional tradeoff. Result: all state rows (idle dots, listening controls, Transcribing spinner, Polishing spinner, empty error row) render simultaneously whenever the surface shows, and the surface itself cannot be hidden in-page. jsdom tests miss it because pill.css is never applied and toBeVisible() only inspects the hidden attribute/inline styles.

> VERIFIER: CONFIRMED, not refuted. Verified by reading the code and by an empirical WKWebView test. (1) pill.css really assigns author-origin display values to every hidden-toggled element: `.pill-surface{display:flex}` (line 33-51) and `.pill-dots,.pill-bars,.pill-status{display:flex}` (lines 84-90); only `.pill-preview[hidden]{display:none}` (161-163) is guarded. pill.tsx (setHidden at 207-209, render at 270-277) relies solely on element.hidden; no other CSS rule (data-state/data-visible) hides the rows. (2) Ran a real WKWebView harness on this Mac (the app's actual engine; app min macOS is 14.0) with the exact pattern: computed display for hidden .pill-surface/.pill-dots/.pill-status was "flex" — the hidden attribute is defeated exactly as claimed, while the guarded .pill-preview and a plain hidden div computed "none". So during any recording the idle dots, listening controls, Transcribing, Polishing and empty error rows all render simultaneously. (3) Partial mitigation: the backend shows/hides the pill NSWindow natively per recording (audio.rs:4622-4630, :3464; window never shown when mode=never), so the "surface never hides in-page" sub-claim is largely masked at the window level — but this does not prevent the stacked-states regression while the window is visible during recording. (4) No plan documents this as a tradeoff (plans/031-pill-micro-bundle-status-ux.md describes mutually exclusive per-state visuals); the lone .pill-preview[hidden] guard shows the mechanism was known but missed for the sibling elements. The jsdom/toBeVisible test-blindness explanation is also consistent: pill.css is never applied in tests.

### [critical] .github/workflows/ci.yml:246 — Windows CI 'Rust tests' step runs run-tests.ps1 from repo root where cargo finds no Cargo.toml

The step is `run: ./src-tauri/run-tests.ps1` with shell pwsh and no working-directory, so the script executes with cwd = GITHUB_WORKSPACE (repo root). Inside src-tauri/run-tests.ps1, `& cargo test --no-run` (line 41) and `& cargo test` (line 92) inherit that cwd. The cargo workspace manifest is src-tauri/Cargo.toml and there is no Cargo.toml at the repo root or any parent, so cargo exits with 'could not find `Cargo.toml`' (code 101); the script propagates it via `exit $LASTEXITCODE`. Every push/PR run of the build-windows job fails at this step — the slice-036 goal of running real Windows tests never executes. Fix: `working-directory: src-tauri` (and drop the src-tauri/ prefix) or cd into src-tauri before invoking.

> VERIFIER: Confirmed, not refuted. In the integration worktree, .github/workflows/ci.yml's build-windows "Rust tests" step is `shell: pwsh` / `run: ./src-tauri/run-tests.ps1` with no working-directory and no defaults.run.working-directory anywhere in the workflow, so cwd is GITHUB_WORKSPACE (repo root). src-tauri/run-tests.ps1 (unchanged from main; the branch diff touches only ci.yml) contains no Set-Location and invokes bare `& cargo test --no-run` / `& cargo test`, which inherit that cwd. There is no Cargo.toml at the repo root (verified) — the only manifest is src-tauri/Cargo.toml — so cargo exits 101 ("could not find Cargo.toml") and the script propagates it via `exit $LASTEXITCODE`, failing the step. plans/036-ci-gates.md does not document this as a tradeoff; it explicitly intends "Windows: cargo test for real" and admits CI could not be verified locally. Sibling steps in the same job (Clippy, Build Tauri) correctly `cd src-tauri` first, confirming this step deviates from the needed pattern. Only softening nuance: the failure is loud (job fails every run), not a silent test skip.

> VERIFIER: Confirmed. The 'Rust tests' step (.github/workflows/ci.yml:244-246) is `shell: pwsh` / `run: ./src-tauri/run-tests.ps1` with no `working-directory` and no `defaults.run.working-directory` anywhere in the workflow, so it executes with cwd = GITHUB_WORKSPACE (repo root); the neighboring Clippy and release-build steps explicitly `cd src-tauri`, confirming the default cwd. src-tauri/run-tests.ps1 contains no Set-Location/Push-Location ($PSScriptRoot is used only for the manifest file path), so `& cargo test --no-run` (line 41) runs from the repo root. There is no Cargo.toml at the repo root or any parent (only src-tauri/Cargo.toml and sidecar/whisper-vulkan/Cargo.toml, neither reachable by cargo's upward manifest search), so cargo exits 101 with 'could not find Cargo.toml' and the script propagates it via `exit $LASTEXITCODE` (lines 43-46), failing the step on every Windows CI run. plans/036-ci-gates.md's goal is 'Windows: cargo test for real' and it explicitly notes CI behavior could not be verified locally ('the real proof is the first CI run') — an acknowledged verification gap, not a documented intentional tradeoff. Main's prior step was `cd src-tauri && cargo test --no-run`; the branch dropped the cd when switching to the script.

### [major] src-tauri/src/parakeet/sidecar.rs:725 — Abandoned stream session leaves sidecar permanently 'stream_busy', breaking all subsequent Parakeet transcription

When the stream control channel closes without an explicit Finalize/Cancel, the driver task just breaks (sidecar.rs:723-726) and never writes `cancel_stream` to the sidecar. This is the path actually taken on user cancel: cancel_recording sets should_cancel_recording, the tap worker's cancelled/stale drain branch hits `StreamTapMsg::Finalize => break` (stream_tap.rs:265) or the timeout break (stream_tap.rs:250-252) WITHOUT calling sink.cancel() (StreamTapMsg::Cancel is dead code, stream_tap.rs:17-18), the sink/ParakeetStreamHandle is dropped, and the driver sees a closed channel. The same abandonment happens if recorder setup fails after the sink factory ran (e.g. WavWriter::create error at recorder.rs:345, before the tap worker spawns at recorder.rs:364). The sidecar keeps `activeStreamSession` set forever (no sidecar-side timeout) and rejects transcribe/load_model/start_stream with `stream_busy` (main.swift:311-312, 733), and Rust has no respawn for that error code (only `Terminated` triggers respawn, sidecar.rs:227-260). Scenario: with streaming/live-preview flags on, user starts a recording and presses Esc to cancel once → every following recording's batch Parakeet transcription fails with stream_busy until the app restarts. Reachable only when the default-off streaming flags / live_preview mode are enabled, but plans promise the batch path is unaffected by stream errors, so this is not a documented tradeoff.

> VERIFIER: Confirmed at every link. (1) sidecar.rs:723-726: closed control channel → bare break with no CancelStream write and no post-loop cleanup; the 30s inactivity branch likewise never writes cancel_stream; ParakeetStreamHandle/ParakeetPreviewStreamSink have no cancelling Drop. (2) stream_tap.rs: StreamTapMsg::Cancel is #[allow(dead_code)] and constructed only in a test; on user cancel the drain branch (Finalize=>break, line 265) or timeout break (250-252) drops the sink without sink.cancel()/finalize(), closing the driver channel. Recorder setup failure after the sink factory (WavWriter::create, recorder.rs ~345) drops the sink the same way. (3) main.swift: activeStreamSession has no timer and while set rejects transcribe/load_model/start_stream/unload_model with stream_busy (lines 311-312, 731-737, 959-960) — even unload_model, the one command that would internally cancel, is blocked. (4) Rust has zero stream_busy handling (grep empty); clear_sidecar fires only on Timeout/"cancelled"; retry only on Terminated; ParakeetManager::shutdown is app-exit only. So one cancelled recording with streaming/live-preview enabled permanently poisons Parakeet batch transcription until restart. Not a documented tradeoff: plan 040 explicitly required "Cancel → cancel()" wiring (left unimplemented) and its acceptance promises "if the stream session errors, recording is unaffected"; plan 042 repeats "Batch paste path untouched." The only mitigation is reachability — the path needs default-off dev flags or a hand-edited transcription_mode=live_preview (audio.rs:4104-4123 reads the store with no capability check), and plan 042's outcome marks the feature dormant — which bounds severity (arguably major→minor for shipped defaults) but the finding already discloses this and it does not meet any refutation criterion.

> VERIFIER: Every code claim verified true. (1) sidecar.rs driver loop: on control-channel close (`let Some(control) = control else { break; }`) it exits without writing cancel_stream; CancelStream is only written in the Cancel arm, and neither ParakeetStreamHandle nor ParakeetPreviewStreamSink has a Drop impl, so an abandoned handle never tears the session down. (2) The cancel path really abandons the handle: cancel_recording sets the flag then stops the recorder; the tap worker's cancelled/stale drain branch handles Finalize with a bare `break` (no sink.cancel()/finalize()), StreamTapMsg::Cancel is #[allow(dead_code)] and never constructed, so the sink drops silently. The recorder-setup-failure variant is also real (sink factory runs before WavWriter::create in recorder.rs). (3) Swift side: activeStreamSession has no timeout and blocks transcribe/load_model/start_stream with stream_busy (main.swift:311-312, 731-737, 960). (4) Rust recovers only on Terminated/Timeout/"cancelled" (dispatch_cancellable + clear_after in sidecar.rs); "stream_busy" is handled nowhere, so the sidecar stays bricked for batch Parakeet until app restart. (5) Not a documented tradeoff: plan 040 explicitly scoped "Cancel → cancel()" and its acceptance requires "Batch path ... identical with flags on; if the stream session errors, recording is unaffected" — this code violates that contract; the 038-era "future cancel wiring slice" comment is a stale deferral, not an accepted caveat. Only caveat: per plan 042's Outcome the feature is shipped-but-dormant (PARAKEET capability is FINAL_ONLY, live-preview UI hidden, activate_live_preview hard-rejects), so today the bug is reachable only via the deliberately-retained dev-override store flags — which the finding itself discloses. That lowers practical severity in the shipped default but does not refute the defect, which becomes user-facing the moment the capability flips.

### [major] src-tauri/src/audio/recorder.rs:735 — Unbounded stream-tap worker join lets a 30s sink finalize blow the 8s stop budget and discard the user's finalized recording

On stop, the recording thread joins the tap worker unbounded (`let _ = worker.join();`, recorder.rs:734-736) after the bounded writer join. The worker's Finalize branch calls ParakeetPreviewStreamSink::finalize() (commands/audio.rs:105-130), which block_on-waits for the sidecar's stream_final with STREAM_FINALIZE_TIMEOUT = 30s (parakeet/sidecar.rs:23,72). stop_recording only waits STOP_JOIN_TIMEOUT = 8s for the recording thread (recorder.rs:72, 802-818), and its doc comment claims it covers every internal sub-budget — the 30s tap finalize is not covered. Scenario: streaming/live-preview enabled, user stops a recording while the sidecar finalize is slow or hung (>~5s: 8s budget minus drain + writer join) → stop_recording returns 'Recording thread failed to stop within timeout', and the stop flow treats a non-finished worker as no-usable-WAV (commands/audio.rs:4808-4813 comment and error path), surfacing an error and dropping the dictation even though the WAV was already finalized on disk 3+ seconds earlier by the bounded writer join.

> VERIFIER: Confirmed, not refuted. The code chain is exactly as claimed: recorder.rs:735 joins the stream-tap worker unbounded inside the recording thread; the worker's Finalize branch (stream_tap.rs:299-304) synchronously calls ParakeetPreviewStreamSink::finalize() (commands/audio.rs:105-130), which block_on-waits up to STREAM_FINALIZE_TIMEOUT=30s (sidecar.rs:23,72). stop_recording waits only STOP_JOIN_TIMEOUT=8s (recorder.rs:72,802-818) and its doc comment budget omits the tap finalize. On timeout the error string matches stop_error_is_unfinalized (recorder.rs:929-930), and commands/audio.rs:4840-4861 drops the recording path and shows "Recording error" — discarding a WAV the bounded writer join had already finalized. No guard bounds the worker join or cancels the stream before stop. Not a documented tradeoff: plans 040/042 explicitly require the batch path to be unaffected by stream errors ("if the stream session errors, recording is unaffected"; "Batch paste path untouched"), which this scenario violates. Only nit: the trigger threshold is ~7.7s of sidecar finalize delay in the typical case (writer join usually completes in ms), not ~5s; this does not change the finding.

> VERIFIER: Not refuted — every mechanical claim verified in current code. recorder.rs:734-736 joins the stream-tap worker unbounded inside the recording thread that stop_recording bounds to STOP_JOIN_TIMEOUT=8s (recorder.rs:72, 802-818); the worker's Finalize branch (stream_tap.rs:299-303) calls ParakeetPreviewStreamSink::finalize (commands/audio.rs:105-130) which block_on-waits the sidecar stream_final under a 30s STREAM_FINALIZE_TIMEOUT (parakeet/sidecar.rs:23,62-80) with nothing able to interrupt it. A finalize slower than ~7.8s therefore makes stop_recording return 'Recording thread failed to stop within timeout', which stop_error_is_unfinalized (recorder.rs:929-931) classifies as no-usable-WAV, and the stop flow (commands/audio.rs:4840-4861) drops the recording path and surfaces 'Recording error' even though the bounded writer join (recorder.rs:732, 3s) already finalized the WAV. This is not a documented tradeoff: the STOP_JOIN_TIMEOUT doc comment (recorder.rs:64-72) enumerates every sub-budget it covers and omits the tap finalize, and plan 040 explicitly requires 'if the stream session errors, recording is unaffected'. The only mitigation is that the path is shipped dormant (EngineStreamCapabilities::PARAKEET is FINAL_ONLY, stream.rs:80-82, hiding the live-preview toggle; dev flags default false) — but plan 042's dormancy is for unrelated EOU quality reasons, the in-tree dev flags (streaming_tap_enabled + streaming_engine_enabled) activate the path today, and the hazard fires unguarded the moment the feature is enabled. Severity could arguably be downgraded from major given dormancy, but the finding itself stands.

### [major] src-tauri/src/parakeet/sidecar.rs:676 — Stream-open ready phase has no deadline while holding the client-wide RwLock — a silent sidecar wedges recording start and every future Parakeet command forever

The spawned session task acquires the ParakeetClient write guard (sidecar.rs:629) and then loops on next_protocol_response() waiting for StreamStarted with NO timeout (sidecar.rs:676-718); open_stream's ready_rx.await (sidecar.rs:815) is equally unbounded, and a Cancel control cannot be observed until the second loop. Every other sidecar interaction is deadline-bounded precisely because a live-but-unresponsive FluidAudio stall is a known field failure (comment at sidecar.rs:452-458 and the dispatch_cancellable doc), but this path is not. If the sidecar accepts start_stream and never replies: (1) the recorder thread is blocked forever inside the factory's block_on (commands/audio.rs:199, invoked at recorder.rs:310-312 before the mic/WAV are even opened), so the recording captures nothing and stop_recording returns 'Recording thread failed to stop within timeout'; (2) the write guard is held forever, and ensure() (sidecar.rs:492) has no deadline around lock acquisition — timed_request only wraps the request future — so every later warmup/status/transcribe/load_model awaits the lock indefinitely instead of erroring, including the next recording attempt's factory load_model, and even ParakeetClient::shutdown() blocks. One wedged stream open permanently disables Parakeet (and every subsequent Parakeet recording start) until app restart. Same exposure applies to the perf-harness CLI stream path (src-tauri/src/cli.rs:600).

> VERIFIER: Verified in current code: the open_stream session task (sidecar.rs:629) holds the ParakeetClient write guard for its whole lifetime, and the pre-StreamStarted ready loop (sidecar.rs:676-718) awaits next_protocol_response() — a bare rx.recv() loop with no deadline (sidecar.rs:314-357) — so a live-but-silent sidecar pends forever; STREAM_INACTIVITY_TIMEOUT only guards the post-ready loop. ready_rx.await (sidecar.rs:815) and manager.rs:247 add no timeout, Cancel is unreachable (control_rx polled only in the second loop and the handle is never returned), and the factory's block_on (commands/audio.rs:199) runs on the recording thread at recorder.rs:310-312 before WAV/stream setup, with the claimed stop-timeout error at recorder.rs:818. ensure() (sidecar.rs:492) and shutdown() (sidecar.rs:826) await the write lock with no deadline (timed_request wraps only the post-lock request future), so one wedged open permanently blocks every later Parakeet command with no recovery path. Not a documented tradeoff: plan 040 specifies 30s inactivity/finalize timeouts but no open deadline, and its acceptance line "if the stream session errors, recording is unaffected" is violated by this scenario. Path is user-reachable via transcription_mode=live_preview (audio.rs:4104-4126). Only softening: load_model succeeds (deadline-bounded) just before start_stream, so the sidecar must stall specifically on stream-session setup — but that is native FluidAudio work, the code's own comments name such stalls a known field failure, and the codebase deadline-bounds every other interaction for exactly this reason.

> VERIFIER: Every element of the finding checks out against the current code. The stream-open ready phase is genuinely unbounded: next_protocol_response() (src-tauri/src/parakeet/sidecar.rs:314-357) is a bare rx.recv().await loop with no deadline, the ready loop at sidecar.rs:676-718 has no timeout branch, and STREAM_INACTIVITY_TIMEOUT only guards the post-StreamStarted loop (line 802); ready_rx.await at line 815 is also unbounded. The session task holds the client-wide write guard (taken at line 629) across the wedged loop, Cancel is only observable in the second loop, and the child handle lives inside the locked slot so nothing can kill a live-but-silent sidecar. ensure() (line 492) awaits the lock with no deadline and timed_request wraps only the request future, so every subsequent Parakeet command — and even shutdown() (line 826) — blocks forever instead of erroring. The recorder thread invokes the factory's block_on (commands/audio.rs:199, recorder.rs:310-312) before the WAV writer and CPAL stream are created, so a wedged open blocks recording start and stop_recording hits the 'Recording thread failed to stop within timeout' path (recorder.rs:818). This is not a documented tradeoff: plan 040 explicitly calls for 'overall session inactivity 30s' timeouts, which the ready phase violates, and no plan documents an unbounded stream open. The scenario is reachable in the field via the user-facing live_preview recording mode (audio.rs:4104-4123), and the codebase itself treats live-but-unresponsive FluidAudio sidecars as a known failure class (dispatch_cancellable doc, sidecar.rs:452-458). The CLI path (cli.rs:600) shares the exposure. Finding stands as reported.

### [major] src/pill.tsx:577 — Pill never queries initial recording state — misses states emitted before its listeners register (Windows first recording shows no pill)

The rewritten pill is purely event-driven: at boot it only invokes get_settings (pill.tsx:578) and subscribes to recording events. It never invokes get_current_recording_state, which the previous implementation did on mount via useRecording (git show main:src/hooks/useRecording.ts, 'Check initial state on mount'). On Windows the pill window is created lazily inside start_recording (src-tauri/src/commands/audio.rs:4622-4630 → window_manager.rs show_pill_window_internal), and the backend emits `recording-state-changed` immediately after build/show (window_manager.rs:~340) plus recording-started via emit_to_all — all while the pill webview is still loading, before its listen() registrations exist, so the events are dropped. Result: for the first recording of every Windows app session the pill stays in 'idle' (invisible in when_recording mode) for the entire recording, only appearing when 'transcription-started' arrives at stop. Same failure applies to any pill webview reload mid-recording. This is a regression versus main, where the mount-time get_current_recording_state fetch recovered the current state after load.

> VERIFIER: Confirmed, not refuted. The rewritten src/pill.tsx invokes only get_settings at boot (lines 484/578) and never get_current_recording_state (grep confirms no reference in the pill entry), whereas main's pill used useRecording's mount-time get_current_recording_state fetch (visible in git show main:src/hooks/useRecording.ts, commented "Check initial state on mount"). On Windows the pill window is created lazily inside start_recording: lib.rs:1192-1232 pre-creates the pill only on macOS, and the default pill_indicator_mode is "when_recording" (settings.rs:105) so the "always" startup path (lib.rs:794) is skipped. During the first recording, Starting/Recording state changes are emitted via plain app.emit before the window exists (audio.rs:4078/4575 — dropped and not queued, since queue_pill_event only runs on the emit_to_window path, window_manager.rs:537), and the creation-time recording-state-changed emit (window_manager.rs:337-353), queued-event flush (:373-377), and recording-started (audio.rs:4649) all fire milliseconds after build(), before the pill webview can load and register listen() handlers — Tauri does not replay events to later-registered listeners. No recovery path exists: the audio-level and stream-preview handlers are gated on visibleState()==="listening" (pill.tsx:294/354), and no further state event arrives until stop. Plans 031/039 do not document this as a tradeoff; plan 031's premise ("state inputs are ALL Tauri events, already emitted to the pill window") is simply wrong for the lazily created window. Only minor overstatement: the failure applies to when_recording mode (the default), not to "always" mode where the window is pre-created at startup — the finding already notes this scoping.

> VERIFIER: Verified, not refuted. (1) src/pill.tsx contains no get_current_recording_state call — only get_settings (line 484) and cancel_recording; state is purely event-driven. (2) Main's pill recovered via useRecording's mount-time get_current_recording_state fetch (src/hooks/useRecording.ts:34-49), which the rewrite dropped. (3) On Windows the pill window is lazily built inside start_recording (lib.rs pre-creates it at startup only under #[cfg(target_os = "macos")]; window_manager.rs:230 'lazy-loaded on recording start'). (4) The state is already Recording (audio.rs:4575) before the window is built (4630); the pill-targeted recording-state-changed emit (window_manager.rs:340) and recording-started emit (audio.rs:4650) fire microseconds/milliseconds after build(), before the WebView2 page loads and registers listen() — Tauri v2 does not buffer events for late listeners, so they are dropped. The pill_event_queue does not guard this: it only queues when the window object is missing (window_manager.rs:527-540), its flush (line 375) runs immediately after creation in the same race window, and update_recording_state uses global app.emit (app_state.rs:251) which is never queued. (5) Impact confirmed: default mode when_recording hides the pill in idle (pill.tsx:261), audio-level is only subscribed once already listening, and no recovery event arrives until transcription-started at stop; the window is hidden not closed afterward, so exactly the first recording per Windows session shows no pill. (6) No plan documents this tradeoff — plan 031 asserts pill state inputs are 'ALL Tauri events, already emitted to the pill window', a premise that is false for the Windows lazy-creation path. Genuine regression versus main.

### [major] src/hooks/useInAppRecordingHotkey.ts:277 — AltGr press spuriously starts modifier-hold recording, and the AltGraph early-return on keyup can leave the hold stuck (recording never stops)

With a Windows bare-modifier hold_to_record binding on Control and focus in an in-app editable field: pressing AltGr (to type @, €, etc.) makes WebView2 synthesize a ControlLeft keydown first, at which point altKey is still false so hasOtherModifierHeld() passes and the modifier_hold branch (lines 243-246) sets activeHoldCode and calls startHold() — recording starts while the user is just typing a character. The isolated_tap path recovers on the synthesized second key ('Any non-matching key while the modifier is held → pendingTap = null', lines 251-255), but nothing clears activeHoldCode there. On release, onKeyUp line 277 does `if (event.isComposing || event.getModifierState?.("AltGraph")) return;` BEFORE stopHold/clearing activeHoldCode — if AltGraph still reads true at the ControlLeft keyup (Alt half still down), the hold is never stopped: recording keeps running until a window blur or a later Control press. Best case (AltGraph already false) it is a spurious sub-second start→stop recording producing a 'recording too short' error per AltGr keystroke. Plan 041 documents adding hold support but has no AltGr caveat, so this is not a documented tradeoff.

> VERIFIER: Not refuted — the finding is confirmed by the actual code. With a Windows bare-modifier hold_to_record binding on Control (configurable via GeneralSettings.tsx:229 + HotkeyInput side capture) and focus in an editable field, AltGr's synthesized ControlLeft keydown passes eventMatchesBareModifier and hasOtherModifierHeld (altKey still false at that instant), and the modifier_hold branch (useInAppRecordingHotkey.ts:243-246) sets activeHoldCode and starts recording with no AltGraph check — unlike the combo path (line 266) which explicitly guards AltGr. The synthesized second key (AltRight) only clears pendingTap (lines 251-255), never activeHoldCode. On keyup, line 277 returns on getModifierState("AltGraph") BEFORE stopHold()/clearing activeHoldCode; since Windows releases the fake ControlLeft while RAlt is still down, AltGraph can read true and the hold is never stopped — recording runs until blur/visibilitychange or a later Control press. Best case is a spurious sub-second start→stop per AltGr keystroke. Plan 041 (F1) mandates hold support with no AltGr caveat, so this is not a documented tradeoff, and nothing elsewhere (including the native keytrigger Windows backend, which has no fake-Ctrl/AltGr filtering) guards it.

> VERIFIER: Verified against the actual code — the finding is accurate and the scenario is reachable. In src/hooks/useInAppRecordingHotkey.ts the modifier_hold keydown branch (lines 236-249) checks only event.repeat and hasOtherModifierHeld (ctrlKey/altKey/shiftKey/metaKey — never AltGraph); on Windows AltGr synthesizes a ControlLeft keydown before the AltRight keydown, at which point altKey is false, so a Control hold binding sets activeHoldCode and calls startHold() — recording starts while the user is typing @/€. The combo path's AltGraph guard (line 266) is unreachable here because the bare-modifier branch returns at line 246. The AltGr protection the author added for the tap path (line 253 clears pendingTap on the synthesized second key) does not clear activeHoldCode or stop the hold — an asymmetry showing oversight, not design. On keyup, line 277 returns on getModifierState("AltGraph") BEFORE stopHold()/clearing activeHoldCode; on AltGr release Windows releases Ctrl first while AltRight is still down, so AltGraph typically reads true and the hold is left stuck (recording continues until blur or a later Ctrl press); best case it's a spurious sub-second recording per AltGr keystroke. Reachability confirmed: ShortcutsSection.tsx (~lines 345-355) lets users bind push-to-talk (hold_to_record + modifier_hold) on Control, and the fallback arms on Windows in editable fields — exactly where AltGr typing happens. Not a documented tradeoff: plans/041-windows-bare-modifier-fix.md has no AltGr caveat for the hold path, and the native keytrigger Windows hook filters only LLKHF_INJECTED, so nothing guards this elsewhere.

### [minor] sidecar/parakeet-swift/Sources/main.swift:1008 — EOU committed/tentative fold diverges from the Rust monotonic gate when the recognizer revises committed text — duplicated preview text and permanent committed-state divergence

In the EOU partial callback, if the accumulated transcript no longer starts with committedPrefix (recognizer revision), Swift sends the FULL transcript as an unconfirmed tentative (main.swift:1006-1010); Rust appends tentative to its stored committed (commands/audio.rs:253), so the pill renders committed + full-transcript with the committed prefix duplicated. Worse, on the next EOU commit Swift unconditionally sets committedPrefix = transcript (main.swift:1020-1021) while Rust REJECTS the non-monotonic commit and keeps its old committed (commands/audio.rs:243-249) — from then on Swift computes tentative tails relative to a prefix Rust never accepted, and every subsequent partial is appended to the wrong committed base (garbled preview) for the rest of the session. Feature is dormant by default (PARAKEET caps final_only per plan 042) but reachable via persisted transcription_mode=live_preview and exercised by cli stream-bench --engine eou.

### [minor] sidecar/parakeet-swift/Sources/main.swift:1033 — Streaming-path FluidAudio calls are not wrapped in withLibraryStdoutRedirected — a single stray CoreML stdout line aborts the stream and (via the missing-teardown bug) wedges the sidecar

Every batch-path library call (loadModel, transcribe, warmup, EOU download/warmup) is wrapped in withLibraryStdoutRedirected because FluidAudio/CoreML print diagnostics to stdout (the repo's own tests model 'E5RT encountered an STL exception' noise). The streaming path is not: SlidingWindowAsrManager.loadModels/startStreaming (main.swift:1033-1034), streamAudio (:1085), EOU appendAudio/processBufferedAudio (:1088-1089), and finish() in finalizeStream (:1110-1112) all run with stdout = protocol pipe. A standalone diagnostic line on stdout during a stream fails parse_response_line, and the Rust loop treats unparseable stdout as fatal (sidecar.rs:794-799 / :346-351): the session errors out mid-recording and the loop breaks without cancel_stream, leaving the sidecar wedged in stream_busy (see the teardown finding).

### [minor] src/pill.tsx:291 — startAudioListener/syncStreamListener can double-subscribe during the async listen() gap, leaking Tauri event listeners

startAudioListener guards with `if (audioUnlisten) return` but audioUnlisten is only assigned when the listen() promise resolves. If the pill flaps listening → non-listening → listening within one listen() registration round-trip (e.g. rapid recording-state-changed transitions), a second listen() is issued while the first is still pending; both resolve while state is 'listening', the second assignment overwrites the first unlisten fn, and the first audio-level listener is never unregistered for the life of the pill window (the pill window is hidden, not destroyed, between recordings, so leaks accumulate across the session — each leaked handler still runs on every audio-level event). syncStreamListener (line 411-429) has the identical pattern for the transcription-stream listener when settings-changed events arrive in quick succession. Low probability per occurrence but unbounded accumulation in a long-lived window.

> VERIFIER: Confirmed, not refuted. In src/pill.tsx, startAudioListener's `if (audioUnlisten) return` guard is ineffective during the async gap: audioUnlisten is only assigned when the listen() promise resolves, and the .then() only checks isDestroyed/visibleState — it never checks whether audioUnlisten was already assigned by a concurrent registration. A listening → non-listening → listening flap within one registration round-trip (backend event bursts like recording-started / recording-too-short / recording-started are delivered ahead of the queued listen() IPC response) issues a second listen(); both resolve while state is 'listening', the second unlisten overwrites the first, and the first audio-level listener becomes permanently unregisterable (destroy() only calls the tracked unlisten). syncStreamListener (lines ~419-429) has the identical flaw and is even easier to trigger: two settings-changed events each run the async readSettings → syncStreamListener, and the second sees streamUnlisten still undefined while the first listen() is pending. The pill window is hidden (hide_pill_window in src-tauri/src/commands/window.rs), not destroyed, between recordings, so leaks accumulate for the session. Impact is bounded to wasted work (leaked stream handler is deduped by the revision check; leaked audio handler re-renders the same value), consistent with the minor severity. Neither plans/031 nor plans/039 documents this as an intentional tradeoff, and no other code guards it.

### [minor] src-tauri/build.rs:68 — rerun-if-changed misses Package.resolved, so a dependency-only bump ships a stale Parakeet sidecar in local `pnpm tauri build`

build.rs emits rerun-if-changed only for ../sidecar/parakeet-swift/Sources, Package.swift, and build.sh (lines 68-70). Package.swift declares FluidAudio with a range (`from: "0.15.2"`), so a `swift package update` bump lands only in Package.resolved. With tauri.macos.conf.json's beforeBuildCommand no longer running `pnpm run sidecar:build` (src-tauri/tauri.macos.conf.json:5), build.rs is now the sole builder on the `pnpm tauri build` path. Scenario: dev pulls a commit that only changes Package.resolved (new FluidAudio, the whole ASR engine), runs `pnpm tauri build` with an existing target dir -> cargo considers the build script fresh, build.sh never runs, and the bundler packages the previously built sidecar binary compiled against the old FluidAudio. CI/release are unaffected (fresh-checkout mtimes force a rerun; the aarch64 release job also builds explicitly), so this bites local release/signing builds. Fix: add `cargo:rerun-if-changed=../sidecar/parakeet-swift/Package.resolved` (and consider adding Package.resolved to the ci.yml swift cache key at line 99 to avoid needless stale-cache restores).

## DISPUTED

### [major] src-tauri/src/audio/stream_tap.rs:265 — Cancelling a recording leaks the sidecar stream session, permanently breaking all subsequent Parakeet transcriptions with stream_busy

Repro (streaming enabled: transcription_mode=live_preview or dev flags): start a Parakeet recording, then press ESC/cancel. cancel_recording (src-tauri/src/commands/audio.rs:7245) sets should_cancel_recording then calls recorder.stop_recording(), which sends StreamTapMsg::Finalize. In run_noop_worker the cancelled branch handles that message with `StreamTapMsg::Finalize => break` (stream_tap.rs:265) — sink.cancel() is never called, and nothing ever constructs StreamTapMsg::Cancel (the variant is marked dead_code at stream_tap.rs:17). The ParakeetPreviewStreamSink is dropped, its ParakeetStreamHandle's control channel closes, and the sidecar driver task just breaks on `let Some(control) = control else { break }` (src-tauri/src/parakeet/sidecar.rs:724) without writing cancel_stream. The Swift sidecar's activeStreamSession therefore stays set forever (only finalize_stream/cancel_stream clear it, main.swift:1104/:1129), and main.swift:311 + :733 reject every subsequent heavy command — including plain batch `transcribe` — with stream_busy. Result: after one cancelled live-preview recording, every following dictation fails at transcription until the sidecar/app is restarted (stream_busy is not the "cancelled" code that triggers sidecar clearing in dispatch). Same leak occurs on the stale-generation and Disconnected worker exit paths (stream_tap.rs:247, :250-252).

> VERIFIER: The code mechanics are accurate (cancel path never sends cancel_stream: stream_tap.rs:265 breaks on Finalize without sink.cancel(), ParakeetStreamHandle has no Drop, sidecar.rs driver breaks on control-channel close without writing cancel_stream, Swift activeStreamSession then blocks all heavy commands with stream_busy, and dispatch only clears the sidecar on Timeout/"cancelled"). However, the triggering scenario cannot occur in this build: the finding's primary path transcription_mode=live_preview is unreachable — EngineStreamCapabilities::PARAKEET is FINAL_ONLY (stream.rs, with an explicit "Dormant until upstream FluidAudio EOU is fixed, see plans/042" comment), activate_live_preview (model.rs:160-166) resets mode to regular and hard-errors before ever persisting live_preview, and the settings UI toggle is hidden unless supports_streaming (ModelsSection.tsx:178). The only remaining trigger is manually editing the settings store to set the dev-only flags streaming_tap_enabled + streaming_engine_enabled, which have no UI surface. Plans document this as intentional: plan 040 says the streaming vertical is "behind default-off flags — NOT a shipped default", plan 042's Outcome records the feature as "shipped-but-dormant ... stays hidden until upstream EOU is fixed", and the un-wired Cancel variant carries an explicit deferral comment ("Future streaming cancel wiring slice will construct this.", stream_tap.rs:17). A latent gap in a documented-dormant, dev-flag-only prototype with a documented deferred cancel-wiring slice is not a live major defect — though the cancel wiring must ship before PARAKEET capabilities flip to supports_streaming.

> VERIFIER: CONFIRMED, not refuted. Verified end-to-end: (1) cancel_recording (commands/audio.rs:7245) sets should_cancel_recording — the same flag the stream_cancelled closure polls (audio.rs:4350-4352) — then recorder.stop_recording() triggers StreamTapFinalizer::finalize(), which only sends StreamTapMsg::Finalize; StreamTapMsg::Cancel is never constructed anywhere (dead_code at stream_tap.rs:17). (2) In the worker's cancelled branch, `StreamTapMsg::Finalize => break` (stream_tap.rs:265) drops the sink without calling sink.cancel(); ParakeetPreviewStreamSink has no Drop impl. Disconnected (:247) and timeout (:250-252) exits also skip cancel. (3) Dropping the sink closes the ParakeetStreamHandle control channel; the sidecar driver task breaks on `let Some(control) = control else { break }` without writing cancel_stream — only the explicit Cancel control branch writes CancelStream. (4) Swift's activeStreamSession is cleared only by finalize_stream/cancel_stream/unload_model/shutdown (main.swift:1104/:1129); while set, isHeavyCommandBlockedDuringStream rejects transcribe, load_model, and start_stream with stream_busy (main.swift:311, :733). (5) No recovery: dispatch clears/respawns the sidecar only on Timeout or code=="cancelled" (sidecar.rs:588-596); stream_busy is a plain SidecarError so no respawn, and ParakeetManager::shutdown (manager.rs:740) has zero callers. Every subsequent Parakeet transcription (streaming and batch fallback) fails until app restart. (6) User-reachable: transcription_mode=="live_preview" alone enables both streaming flags (audio.rs:4104-4123). (7) Not a documented tradeoff — plans/040-parakeet-streaming-vertical.md:70 explicitly specifies "Cancel → cancel()" wiring and plans/042-eou-streaming-live-preview.md:66 promises cancel mid-way yields a clean error with mode staying regular; the implementation violates both, and no plan documents the stream_busy lockout consequence.

### [major] .github/workflows/ci.yml:108 — FFmpeg dist cache restore-keys + existence-only ensure script serves and re-pins stale ffmpeg binaries

The new caches (macOS lines 103-109, Windows lines 179-185) key on hashFiles('scripts/ensure-ffmpeg-sidecar.cjs') but add restore-keys '...-ffmpeg-'. scripts/ensure-ffmpeg-sidecar.cjs short-circuits purely on file existence (macOS: `if (arm64Present && x64Present) ... return` ~line 186; Windows: `if (fs.existsSync(ffmpegDst) && fs.existsSync(ffprobeDst)) ... return` ~line 296) and never re-verifies checksums of already-present files. Scenario: bump the pinned ffmpeg URL/SHA in the script -> cache key changes -> exact miss -> restore-keys restores the OLD dist -> ensure step sees binaries present and keeps them -> post-job cache save stores the stale binaries under the NEW key permanently. The new pin (including a broken URL or wrong hash) is never exercised in CI — the 'Ensure FFmpeg sidecar' step passes vacuously — and CI preview artifacts keep bundling the old ffmpeg indefinitely. Fix: drop restore-keys for this cache, or make the ensure script validate existing binaries against the pinned SHA256 before short-circuiting.

> VERIFIER: Confirmed, not refuted. ci.yml lines 103-109 (macOS) and 179-185 (Windows) key the sidecar/ffmpeg/dist cache on hashFiles('scripts/ensure-ffmpeg-sidecar.cjs') but add restore-keys '{os}-{arch}-ffmpeg-'. The ensure script short-circuits purely on file existence (macOS: arm64Present && x64Present at lines 176-186; Windows: fs.existsSync checks at lines 294-300) and only runs verifyChecksum on fresh-download paths (lines 239-240, 328), never on already-present files. So a pin/URL bump changes the cache key, restore-keys serve the old dist, the ensure step passes vacuously, and actions/cache@v4 saves the stale binaries under the new key at job end (primary key missed), making the staleness permanent and the new pin never exercised in CI. No guard exists elsewhere in ci.yml (no SHA env overrides, no re-verification step). plans/036-ci-gates.md only says to cache dist "keyed on the ensure-ffmpeg script hash" — restore-keys and the stale-restore hazard are not documented, and restore-keys actually defeat the plan's stated invalidation design. Only caveat: impact is CI/preview-artifact-only since release.yml uses no dist cache and always downloads+verifies fresh, which the finding itself correctly scopes.

> VERIFIER: The cache-restore mechanism the finding describes exists in ci.yml, but its claimed consequences cannot occur. (1) CI preview artifacts never bundle ffmpeg: ci.yml runs `cargo build --release` (never `tauri build`) and uploads only the bare executables (src-tauri/target/release/voicetypr, D:/cargo-target/release/voicetypr.exe); ffmpeg is bundled via Tauri externalBin only in release.yml and store-msix.yml. In ci.yml the cached dist merely satisfies existence assertions in src-tauri/build.rs — the binaries are never run or packaged, so 'stale ffmpeg bundled indefinitely' is factually wrong. (2) 'The new pin is never exercised in CI' is guarded elsewhere: store-msix.yml explicitly triggers on PR changes to scripts/ensure-ffmpeg-sidecar.cjs (paths filter line 11), has no ffmpeg dist cache, and runs the ensure script fresh with SHA256 verification (build-msix-store.ps1:42); all three release.yml jobs (lines 153/277/389) also run it uncached with checksum verification, so a broken or stale pin can never reach shipped artifacts — a bad pin fails the release/store build loudly. (3) Caching sidecar/ffmpeg/dist keyed on the script hash is the documented intent of plans/036-ci-gates.md change 4. The residual issue — ci.yml alone would not smoke-test a macOS pin bump immediately — is a minor CI-coverage hygiene nit, not the major stale-binary-shipping defect claimed.

### [minor] sidecar/parakeet-swift/Sources/main.swift:733 — Busy-state gap: 'warmup' is not rejected while a stream session is active

isHeavyCommandBlockedDuringStream (main.swift:731-738) blocks load/unload/delete/transcribe/diarize/downloads/warmup_eou/start_stream during an active stream, but omits 'warmup'. A warmup mid-session runs a full 1s batch transcription on the SAME loaded AsrModels concurrently with the sliding-window session's background processing and blocks the serial stdin event loop, delaying audio_chunk handling and backing up the pipe. Currently unreachable from the shipped Rust host (ParakeetClient holds the sidecar write lock for the whole stream), so this is a protocol-hardening gap rather than a live crash — but any second writer (tests, future host changes) hits it.

> VERIFIER: The observation is textually correct ('warmup' is absent from isHeavyCommandBlockedDuringStream at sidecar/parakeet-swift/Sources/main.swift:733), but the failure scenario is unreachable and guarded elsewhere. The Rust host is the sidecar's only stdin writer, and ParakeetClient::open_stream (src-tauri/src/parakeet/sidecar.rs:601-825) holds the exclusive inner.write() RwLock guard for the entire stream session, while every other command path (send, send_with_progress_and_cancel) must acquire that same write guard — so a warmup issued mid-stream blocks on the lock and only reaches the sidecar after the session ends. On top of that, ParakeetManager::warmup (src-tauri/src/parakeet/manager.rs:613-616) skips warmup entirely when recording/transcription is active. The finding itself concedes it is 'currently unreachable from the shipped Rust host' and only harms hypothetical future writers/tests, which is protocol-hardening advice, not a defect; plan 033 documents the host-side skip-when-busy design intent.
